#!/usr/bin/env bash

set -euo pipefail

if ! command -v mise >/dev/null 2>&1; then
    echo "mise is required. Install mise and run 'mise bootstrap'." >&2
    exit 1
fi

cd "$(dirname "$0")/.."
exec mise bootstrap
